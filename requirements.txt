# pip requirements for the cervical cancer risk prediction project.
#
# Install with: pip install -r requirements.txt
#
# Prefer environment.yml (conda) if available - it also pins the Python
# version. This file lists direct dependencies only, pinned to a verified
# working environment (Python 3.11.14).
#
# Note: `pip freeze` is not used here because inside a conda environment it
# emits local `file:///` build paths that are not installable elsewhere.

# Core scientific stack
numpy==2.4.6
pandas==3.0.5
scipy==1.17.1
scikit-learn==1.9.0
statsmodels==0.15.0

# Class imbalance handling (SMOTE)
imbalanced-learn==0.14.2

# Visualisation
matplotlib==3.11.1
seaborn==0.13.2

# Deep learning (pulls in keras==3.15.1)
tensorflow==2.21.0

# Notebook environment
jupyter==1.1.1
jupyterlab==4.6.3
ipykernel==7.3.0

# Testing
pytest==9.1.1
